# Outlook installation

The first version uses a guided manual flow because there is no standard Microsoft Graph signature-management endpoint. Copy the rendered signature and paste it into Outlook's signature settings.
