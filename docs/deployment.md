# Deployment

Build with `npm run build`. GitHub Pages deployment is configured by `.github/workflows/deploy.yml`. Set `VITE_GOOGLE_CLIENT_ID` as a repository Actions variable if Gmail installation is enabled in the deployed build.
